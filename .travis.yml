language: node_js
node_js:
-10.15.3
os: osx
cache: npm

before_install:
install:
  - yarn install
script:
  - yarn test
  - yarn run build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on: 
    branch: master



